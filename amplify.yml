version: 1
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - NEXT_PRIVATE_STANDALONE=true
        - yarn build

  artifacts:
    baseDirectory: standalone
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
